import React from 'react';

const WrapperSingleProduct = ({children}) => {
    return (
        <div className='container'>
            {children}
        </div>
    );
};

export default WrapperSingleProduct;